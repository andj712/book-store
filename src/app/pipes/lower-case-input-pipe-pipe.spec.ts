import { LowerCaseInputPipePipe } from './lower-case-input-pipe-pipe';

describe('LowerCaseInputPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LowerCaseInputPipePipe();
    expect(pipe).toBeTruthy();
  });
});
